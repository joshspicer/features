{
    "name": "OCI Container Utils",
    "id": "oci-utils",
    "version": "1.0.0",
    "description": "Installs useful tools for working with OCI containers, namely skopeo and oras",
    "options": {
        "orasVersion": {
            "type": "string",
            "proposals": [
                "0.15.1"
            ],
            "default": "0.15.1",
            "description": "Version of oras to install.  See https://oras.land/cli for details."
        },
        "skopeoVersion": {
            "type": "string",
            "enum": [
                "latest"
            ],
            "default": "latest",
            "description": "Version of skopeo to install. Currently only installs the latest from apt."
        }
    }
}
